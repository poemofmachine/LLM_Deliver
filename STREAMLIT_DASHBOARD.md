# 🎨 Streamlit 대시보드 - 시각적 프로세스 UI 가이드

> **프로세스를 시각적으로 보면서 메모를 관리하세요!**

---

## 📑 목차
1. [대시보드란?](#대시보드란)
2. [설치 및 실행](#설치-및-실행)
3. [기능 설명](#기능-설명)
4. [화면 구성](#화면-구성)
5. [자주 묻는 질문](#자주-묻는-질문)

---

## 대시보드란?

**Streamlit 대시보드**는 FastAPI 서버와 연결되는 **웹 기반 UI**입니다.

### 🎯 할 수 있는 것들:

✅ **메모 저장** - 프로세스를 단계별로 시각화하며 저장
✅ **메모 불러오기** - 실시간으로 Google Docs에서 조회
✅ **워크스페이스 관리** - 개인/팀 워크스페이스 생성 및 관리
✅ **프로세스 흐름도** - PUSH/PULL 과정을 다이어그램으로 표시

### 🎨 장점:

| 항목 | 설명 |
|------|------|
| 🔴 **시각적** | 메모 저장/불러오기 과정을 단계별로 볼 수 있음 |
| 🟢 **직관적** | 명확한 버튼과 입력 필드로 사용하기 쉬움 |
| 🟡 **빠름** | Python만으로 작성되어 개발이 빠름 |
| 🔵 **응답성** | 실시간으로 처리 과정을 표시 |

---

## 설치 및 실행

### 준비물

✅ FastAPI 서버가 실행 중이어야 함
✅ Python 3.10 이상

### 1단계: 의존성 설치

```bash
# clients/python 디렉토리로 이동
cd clients/python

# Streamlit 및 필요한 패키지 설치
pip install streamlit requests python-dotenv
```

또는 requirements.txt에서 설치:

```bash
pip install -r requirements.txt
```

### 2단계: FastAPI 서버 실행 (필수!)

**새 터미널에서:**

```bash
cd api_server_v2
uvicorn app.main:app --reload --port 8000
```

> ✅ "Uvicorn running on http://127.0.0.1:8000" 메시지가 보이면 OK!

### 3단계: Streamlit 대시보드 실행

**다른 터미널에서:**

```bash
cd clients

# Streamlit 실행
streamlit run streamlit_dashboard.py
```

### 4단계: 브라우저에서 확인

자동으로 브라우저가 열리며, 다음 주소에서 접속 가능:

```
http://localhost:8501
```

> 💡 **첫 실행이면 몇 초 기다려주세요**

---

## 기능 설명

### 📊 메인 대시보드

화면 상단에 서버 상태가 표시됩니다:

```
✅ 서버 연결됨    |    🔗 http://localhost:8000    |    ✅ API 토큰 설정됨
```

### 🔴 탭 1: 📤 메모 저장 (PUSH)

**목적**: 메모를 Google Docs에 저장하면서 프로세스를 시각적으로 확인

#### 단계:

**1️⃣ 워크스페이스 선택**
```
📁 워크스페이스 선택
└─ 드롭다운 메뉴에서 워크스페이스 선택
```

**2️⃣ 저장 위치 선택**
```
📍 저장 위치
├─ personal (개인 문서)
└─ team (팀 문서)
```

**3️⃣ 메모 입력**
```
📝 저장할 메모 입력
└─ 텍스트 박스에 메모 붙여넣기 또는 입력
```

**4️⃣ 저장 버튼 클릭**
```
💾 Google Docs에 저장
```

#### 🎬 프로세스 시각화:

저장 버튼을 클릭하면 다음과 같이 단계별로 표시됩니다:

```
⏳ 저장 프로세스 진행 중...

✅ 📝 메모 유효성 검사       (완료)
✅ 🔐 인증 확인              (완료)
✅ 💾 로컬 DB 저장           (완료)
✅ ☁️ Google Docs 동기화    (완료)
✅ ✅ 저장 완료!              (완료)

🎉 메모가 성공적으로 저장되었습니다!

저장 시간: 14:23:45    |    저장된 메모 길이: 245 글자
```

**각 단계의 의미:**

| 단계 | 설명 |
|------|------|
| 📝 유효성 검사 | 메모가 비어있는지, 형식이 맞는지 확인 |
| 🔐 인증 확인 | Google Docs 접근 권한이 있는지 확인 |
| 💾 DB 저장 | 로컬 SQLite 데이터베이스에 저장 |
| ☁️ Google Docs 동기화 | Google Docs에 메모 추가 |
| ✅ 완료 | 모든 과정이 성공적으로 완료됨 |

---

### 🟢 탭 2: 📥 메모 불러오기 (PULL)

**목적**: Google Docs에서 최신 메모를 불러옵니다.

#### 단계:

**1️⃣ 워크스페이스 선택**

**2️⃣ 불러올 위치 선택**
```
📍 불러올 위치
├─ personal (개인 문서)
└─ team (팀 문서)
```

**3️⃣ 불러오기 버튼 클릭**
```
📥 메모 불러오기
```

#### 🎬 프로세스 시각화:

```
⏳ 불러오기 프로세스 진행 중...

🔐 인증 확인    |    ☁️ Google Docs 조회    |    📊 메타데이터 파싱    |    ✅ 완료

✅ 메모를 성공적으로 불러왔습니다!

📄 불러온 메모
────────────────────────────────
[HANDOFF]
...메모 내용...
────────────────────────────────

수정 시간: 2024-11-17    |    리비전: 1    |    카테고리: WORK

📖 [Google Docs에서 보기](링크)
```

**결과 정보:**

| 정보 | 설명 |
|------|------|
| 수정 시간 | 메모를 마지막으로 수정한 시간 |
| 리비전 | 메모의 버전 번호 |
| 카테고리 | 자동으로 분류된 카테고리 (WORK, MEETING 등) |
| 링크 | Google Docs에서 직접 보기 |

---

### 🟡 탭 3: 🏢 워크스페이스 관리

**목적**: 워크스페이스를 생성하고 관리합니다.

#### 기존 워크스페이스 보기:

```
기존 워크스페이스
──────────────────
📁 개인 작업        personal
📁 팀 프로젝트      team
```

#### 새 워크스페이스 생성:

```
새 워크스페이스
──────────────────
이름: [입력 필드]
스코프: ○ personal  ● team

[생성 버튼]
```

**입력값:**

| 필드 | 예시 | 설명 |
|------|------|------|
| 이름 | "팀 마케팅" | 워크스페이스 이름 |
| 스코프 | personal/team | 개인용 또는 팀용 |

**성공 메시지:**
```
✅ 팀 마케팅 워크스페이스 생성됨!
```

---

### 🔵 탭 4: 📊 프로세스 흐름도

**목적**: PUSH/PULL의 전체 과정을 다이어그램으로 시각화합니다.

#### PUSH (메모 저장) 흐름:

```
┌─────────────────┐
│  메모 입력      │
└────────┬────────┘
         │
┌────────▼────────┐
│ 유효성 검사     │
└────────┬────────┘
         │
┌────────▼────────┐
│ 인증 확인       │
└────────┬────────┘
         │
┌────────▼────────┐
│ DB 저장         │
└────────┬────────┘
         │
┌────────▼────────┐
│ Google Docs     │
│ 동기화          │
└────────┬────────┘
         │
┌────────▼────────┐
│ ✅ 완료         │
└─────────────────┘
```

#### PULL (메모 불러오기) 흐름:

```
┌─────────────────┐
│ 불러오기 요청  │
└────────┬────────┘
         │
┌────────▼────────┐
│ 인증 확인       │
└────────┬────────┘
         │
┌────────▼────────┐
│ Google Docs     │
│ 조회            │
└────────┬────────┘
         │
┌────────▼────────┐
│ 메타데이터      │
│ 파싱            │
└────────┬────────┘
         │
┌────────▼────────┐
│ ✅ 완료         │
└─────────────────┘
```

---

## 화면 구성

### 🎨 색상 코드 설명

| 색상 | 의미 | 예시 |
|------|------|------|
| 🟢 초록 | 진행 중/완료 | ✅ 저장 완료 |
| 🔴 빨강 | 오류 | ❌ 저장 실패 |
| 🟡 노랑 | 경고 | ⚠️ API 토큰 미설정 |
| 🔵 파랑 | 정보 | ℹ️ 워크스페이스가 없습니다 |

### 🎯 각 요소의 의미

```
┌─────────────────────────────────────────┐
│ 🧠 Memory Hub Dashboard                 │ ← 페이지 제목
│ *AI 메모리를 Google Docs에 저장하세요*  │ ← 설명
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ ✅ 서버 연결됨                          │ ← 서버 상태
│ 🔗 http://localhost:8000                │ ← 서버 주소
│ ✅ API 토큰 설정됨                      │ ← API 토큰 상태
└─────────────────────────────────────────┘

[📤 메모 저장] [📥 메모 불러오기] [🏢 워크스페이스] [📊 프로세스 흐름도]
↑ 탭 메뉴로 각 기능 선택
```

---

## 자주 묻는 질문

### ❓ "Streamlit이 실행되지 않아요"

**확인사항:**

1. Streamlit 설치 확인
   ```bash
   pip list | grep streamlit
   ```

2. FastAPI 서버가 실행 중인지 확인
   ```
   http://localhost:8000 에서 서버 상태 확인
   ```

3. 다시 실행
   ```bash
   streamlit run streamlit_dashboard.py
   ```

### ❓ "서버 연결 실패 메시지가 뜨는데?"

**해결법:**

```bash
# 다른 터미널에서 FastAPI 서버 실행
cd api_server_v2
uvicorn app.main:app --reload
```

> ✅ "Uvicorn running on..." 메시지가 보일 때까지 기다리세요

### ❓ "메모 저장 시 오류가 나요"

**확인사항:**

1. `.env` 파일 확인
   ```env
   DOC_ID="실제 Google Docs ID?"
   WEBAPP_URL="http://localhost:8000"
   API_TOKEN="실제 토큰?"
   ```

2. Google Docs 접근 권한 확인
   - 자신의 계정으로 생성한 문서인가?

3. Google Apps Script 배포 확인
   - 웹 앱으로 배포되었는가?

### ❓ "텍스트 박스에 붙여넣기가 안 되는데?"

**해결법:**

1. Streamlit은 브라우저 기반이므로 [Ctrl + V] 사용
2. 다른 브라우저 시도 (Chrome 권장)
3. JavaScript 활성화 확인

### ❓ "대시보드를 외부에서도 접속하고 싶어요"

```bash
# 모든 IP에서 접속 가능하게 실행
streamlit run streamlit_dashboard.py \
  --server.address=0.0.0.0 \
  --server.port=8501
```

그 후 다른 컴퓨터에서:
```
http://당신의IP:8501
```

### ❓ "대시보드를 커스터마이징하고 싶어요"

`streamlit_dashboard.py` 파일을 편집하세요!

**예: 새로운 탭 추가**

```python
with st.tabs([..., "새 탭"]) as new_tab:
    with new_tab:
        st.header("새 기능")
        # 여기에 코드 추가
```

**예: 색상 변경**

```python
st.success("원하는 색상")      # 초록 (성공)
st.error("원하는 색상")        # 빨강 (오류)
st.warning("원하는 색상")      # 노랑 (경고)
st.info("원하는 색상")         # 파랑 (정보)
```

---

## 🔧 Streamlit 팁 & 트릭

### 📊 다양한 UI 요소

```python
# 입력
st.text_input("텍스트")
st.text_area("여러 줄 텍스트")
st.selectbox("드롭다운")
st.radio("라디오 버튼")
st.slider("슬라이더")

# 표시
st.write("텍스트")
st.metric("메트릭", 값)
st.progress(0.5)  # 진행 바

# 레이아웃
col1, col2 = st.columns(2)  # 2개 열
with col1:
    st.write("왼쪽")
with col2:
    st.write("오른쪽")

# 피드백
st.success("성공!")
st.error("오류!")
st.warning("경고!")
st.info("정보")
```

### 🔄 실시간 업데이트

```python
import streamlit as st
import time

placeholder = st.empty()

for i in range(10):
    with placeholder.container():
        st.write(f"진행 중: {i+1}/10")
        time.sleep(1)
```

### 💾 세션 상태 유지

```python
if "counter" not in st.session_state:
    st.session_state.counter = 0

if st.button("증가"):
    st.session_state.counter += 1

st.write(f"카운트: {st.session_state.counter}")
```

---

## 📚 다른 방법들 비교

### 다른 시각화 방법이 궁금하신가요?

| 방법 | 장점 | 단점 | 추천 |
|------|------|------|------|
| **Streamlit** (현재) | 빠른 개발, Python만 사용 | 커스터마이징 제한 | ✅ 초보자 |
| **FastAPI + React** | 완전한 제어, 최신 기술 | 개발 시간 오래 걸림 | 👍 고급 |
| **Gradio** | 간단함 | 기능 제한 | 👌 ML 프로젝트 |
| **Flask** | 가벼움 | 많은 코드 필요 | 👌 미니 프로젝트 |

---

## 🆘 더 많은 도움

**문제가 해결되지 않았나요?**

- **Streamlit 공식 문서**: https://docs.streamlit.io/
- **GitHub Issues**: https://github.com/MediumsKor/LLM_Deliver/issues
- **Streamlit Community**: https://discuss.streamlit.io/

---

## 📝 다음 단계

✅ Streamlit 대시보드 실행 완료!

다음으로 할 수 있는 것들:

1. **대시보드 커스터마이징** - 색상, 레이아웃 변경
2. **새로운 탭 추가** - 통계, 내보내기 등
3. **FastAPI + React로 고도화** - 더 강력한 UI 만들기

---

**Happy Visualizing! 🎨✨**
