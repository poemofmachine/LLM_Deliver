# 🚀 API Server (Google Apps Script) v2.0

이 스크립트는 GDoc ID와 API 토큰을 URL 파라미터로 받아, 해당 문서의 최신 `[HANDOFF]` 블록을 동적으로 파싱하는 범용 API입니다.

## 🏁 빠른 배포 가이드
1.  Google Apps Script (script.google.com)에서 새 프로젝트를 생성합니다.
2.  `code.gs` 파일의 내용을 복사하여 붙여넣습니다.
3.  **[중요] API 토큰 설정 (보안):**
    * [파일] > [프로젝트 속성] > [스크립트 속성] 탭으로 이동합니다.
    * `API_TOKEN` 이라는 속성을 추가하고, 값에 C님만의 **비밀번호** (예: `C_SECRET_1234`)를 입력하고 저장합니다. (이것이 `.env`의 `API_TOKEN`이 됩니다.)
4.  [배포] > [새 배포] > [유형: 웹 앱]을 선택합니다.
5.  [액세스 권한] > **[모든 사용자(Anonymous)]**로 설정합니다. (우리가 스크립트 속성으로 토큰을 검사하므로 안전합니다.)
6.  [배포]를 누르고 생성된 **웹 앱 URL**을 복사합니다.

## 💡 API 사용법
배포된 URL 뒤에 `docId`와 `token`을 붙여 호출합니다.
`[웹 앱 URL]?docId=[GDoc ID]&token=[스크립트 속성에 저장한 토큰]`